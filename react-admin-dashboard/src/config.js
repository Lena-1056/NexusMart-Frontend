// Admin Dashboard API Configuration
// The admin-service runs on port 8084 and handles ALL admin endpoints
// directly from PostgreSQL. No API gateway needed.

export const API = 'http://localhost:8084';
